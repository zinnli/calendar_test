import styled from "@emotion/styled";

export const Modal = styled.div`
  position: fixed;
  display: flex;
  align-items: center;
  width: 100vw;
  height: calc(var(--vh, 1vh) * 100);
  z-index: 1005;
`;
